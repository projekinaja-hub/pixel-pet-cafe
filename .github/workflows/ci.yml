name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  test:
    # SpriteKit/AppKit — must be macOS, not ubuntu
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4

      - name: Swift version
        run: swift --version

      - name: Cache build
        uses: actions/cache@v4
        with:
          path: .build
          key: ppc-spm-${{ hashFiles('Package.swift') }}-${{ hashFiles('Sources/**/*.swift') }}
          restore-keys: ppc-spm-

      - name: Build
        run: swift build

      # Plain `swift test`, NOT tools/test.sh. That wrapper exists for one
      # developer machine, where XCTest needs explicit DYLD paths and an
      # --enable-xctest flag that older toolchains don't even have
      # ("error: Unknown option '--enable-xctest'"). A clean runner needs
      # neither.
      - name: Test
        run: swift test

      # Proves the app bundle still assembles — catches Info.plist/resource
      # breakage that plain `swift build` cannot see. Unsigned: CI has no
      # signing identity, and signing isn't what's under test here.
      - name: Build app bundle
        run: swift build -c release
